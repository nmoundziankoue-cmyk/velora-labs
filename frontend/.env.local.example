# URL du backend Velora (FastAPI).
# En dev local : http://localhost:8000 (valeur par défaut si non définie).
# En prod (Vercel) : l'URL Render du backend déployé, ex:
# https://velora-backend.onrender.com
NEXT_PUBLIC_API_BASE=http://localhost:8000
